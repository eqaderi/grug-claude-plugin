## Grug engineering for this repo

Follow the global grug engineering instructions.

Repo-specific rules:
- Prefer existing components, tokens, hooks, services, utilities, and tests.
- Do not create new shared abstractions for one-off feature work.
- Do not add dependencies without checking existing packages and explaining the tradeoff.
- Do not change architecture unless the task explicitly requires it.
- For Figma work, match the design using existing design-system primitives first.
- If a new pattern is needed, explain why the existing pattern does not fit.

Useful repo facts to fill in:
- Package manager:
- Install command:
- Dev command:
- App URL:
- Storybook URL:
- Test command:
- E2E command:
- Design tokens path:
- Shared components path:
